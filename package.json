{
  "name": "ltds-ops-monorepo",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "check": "npm --prefix apps/delivery run check && npm --prefix apps/operations run check",
    "test": "npm --prefix apps/delivery test && npm --prefix apps/operations test",
    "build": "npm --prefix apps/delivery run build && npm --prefix apps/operations run build"
  }
}
