# Catalog promotion staging checkpoint — 2026-09-23

This is a default-off infrastructure checkpoint, **not** client catalog or portal activation. Operations PR 114 remains draft and Project Alpha PR 190 remains unmerged. Production Workers, Project Alpha instances, client grants, and public links were not changed by this checkpoint.

- Client staging D1 `client-data-staging` had only migration `0214_ops_inventory_catalog_staging.sql` pending. A private D1 export was made before applying it; export SHA-256: `755b0dc5bb69a3e61ba74a8785f86a075727affba96520d1e97bfd32aca320fc`. The migration succeeded and the three new catalog staging tables exist. Operations staging D1 had no pending migration. No catalog source was enrolled.
- The ignored local Wrangler staging files were backed up and regenerated from the checked-in templates and staging values. Staging preflight passed. Client and Operations typechecks, builds, and Wrangler deployment dry-runs passed. Focused catalog tests passed: Operations 28/28; Client 20/20. The release/staging invariant suite passed 45/45.
- Client staging Worker `ledgetop-clients-staging` deployed as version `8fbc634d-fa70-4ccd-84e1-3540689ee47d`. Operations staging Worker `ledgetop-ops-staging` deployed after Client as version `d7b0d67c-c9d5-4dfb-9144-a881d3421717`. Operations deployment created its configured staging thumbnail container application with zero instances; no production container was changed.
- The staging Workflow `ledgetop-ops-catalog-promotion-staging` was triggered once with both Operations catalog coordinator flags disabled. Instance `catalog-stage-default-off-20260923-0319` completed in one `stage-project-alpha-catalog` step, returned nested `{"status":"disabled"}`, and showed no retry. Client portal, request, and catalog promotion flags remain disabled.
- Unauthenticated staging health: `client-staging` returned HTTP 200; `delivery-staging` and `ops-staging` redirected to Cloudflare Access as configured. This does not prove an authenticated portal workflow. The unauthenticated PA staging catalog route returned 404; with a default-off route, that cannot distinguish missing deployment from disabled flag.
- A separate isolated branch combining PR 114 with the portal Home UI from PR 109 passed 240 local desktop/mobile browser tests, including opaque public-link regressions. No combined branch was deployed or merged. Existing production public-link rows and URLs were not queried or rewritten.

Remaining gates before activation or main merge:

1. Confirm PA PR 190 is deployed to staging and enable its default-off catalog read flag there. Verify the bound, scoped API key and a complete catalog snapshot; do not infer readiness from the unauthenticated 404.
2. Inventory and explicitly approve the exact staging source registry identity, current Client checkpoint sequence, and reviewed snapshot hash. Run the documented stage → human review → exact-hash promote ceremony with default-off rollback evidence before enabling any client-facing catalog read.
3. Complete authenticated portal service/grant and public-link acceptance, then review the stacked Ops PRs and PA owner checkpoint. Catalog availability alone is not a client entitlement or a project grant.
4. Keep PA production changes behind the agreed owner deployment/sign-in checkpoint. Preserve the existing `/s`, `/client-share`, incoming `/r`, and Viewer link contracts through cutover.
