import { useCallback, useEffect, useMemo, useState } from "react";
import type { Permission, SessionUser } from "@ltds/shared";
import { Brand, Card, EmptyState, Loading, StatusPill } from "@ltds/ui";
import { api, setCsrf } from "./api";

interface Session{user:SessionUser;csrfToken:string;timezone:string;mapStyleUrl:string|null}
type Page="dashboard"|"operations"|"projects"|"tasks"|"airspace"|"delivery"|"team"|"administration";
const NAV:Array<{page:Page;label:string;permission:Permission}>=[{page:"dashboard",label:"Dashboard",permission:"dashboard.view"},{page:"operations",label:"Operations",permission:"operations.view"},{page:"projects",label:"Projects",permission:"projects.view"},{page:"tasks",label:"Tasks",permission:"tasks.view"},{page:"airspace",label:"Airspace",permission:"airspace.view"},{page:"delivery",label:"Delivery",permission:"delivery.browse"},{page:"team",label:"Team",permission:"team.view"},{page:"administration",label:"Administration",permission:"integrations.manage"}];
function pathPage():Page{const value=location.pathname.split("/").filter(Boolean)[0] as Page;return NAV.some(item=>item.page===value)?value:"dashboard";}
function allowed(user:SessionUser,permission:Permission){return user.permissions.includes(permission);}
function date(value?:string|null){if(!value)return"Not scheduled";const parsed=new Date(value);return Number.isNaN(parsed.getTime())?value:parsed.toLocaleString([],{dateStyle:"medium",timeStyle:"short"});}
function bytes(value:number){if(value<1024)return`${value} B`;const units=["KB","MB","GB","TB"];let size=value/1024,index=0;while(size>=1024&&index<units.length-1){size/=1024;index++;}return`${size.toFixed(size>=10?0:1)} ${units[index]}`;}

export function OperationsApp(){const[session,setSession]=useState<Session|null>(null),[error,setError]=useState(""),[page,setPage]=useState<Page>(pathPage());useEffect(()=>{api<Session>("/api/session").then(value=>{setCsrf(value.csrfToken);setSession(value);if(!allowed(value.user,NAV.find(item=>item.page===page)!.permission)){const first=NAV.find(item=>allowed(value.user,item.permission));if(first)navigate(first.page);}}).catch(caught=>setError(caught.message));},[]);useEffect(()=>{const pop=()=>setPage(pathPage());addEventListener("popstate",pop);return()=>removeEventListener("popstate",pop);},[]);function navigate(next:Page){setPage(next);history.pushState(null,"",next==="dashboard"?"/":`/${next}`);window.scrollTo(0,0);}if(error)return <main className="fatal"><EmptyState title="Operations unavailable" detail={error}/></main>;if(!session)return <main className="fatal"><Loading/></main>;const props={session};return <div className="ops-shell"><header className="ops-header"><Brand product="Operations"/><nav>{NAV.filter(item=>allowed(session.user,item.permission)||(item.page==="administration"&&session.user.permissions.some(permission=>["roles.manage","integrations.manage","audit.view"].includes(permission)))).map(item=><button key={item.page} className={page===item.page?"active":""} onClick={()=>navigate(item.page)}>{item.label}</button>)}</nav><div className="profile"><span>{session.user.displayName.slice(0,1).toUpperCase()}</span><div>{session.user.displayName}<small>{session.user.email}</small></div></div></header><main className="ops-main"><PageHeading page={page}/>{page==="dashboard"&&<Dashboard {...props}/>} {page==="operations"&&<Operations {...props}/>} {page==="projects"&&<Projects {...props}/>} {page==="tasks"&&<Tasks {...props}/>} {page==="airspace"&&<Airspace/>} {page==="delivery"&&<Delivery {...props}/>} {page==="team"&&<Team {...props}/>} {page==="administration"&&<Administration {...props}/>}</main></div>}

function PageHeading({page}:{page:Page}){const copy:Record<Page,[string,string]>={dashboard:["Operations dashboard","Assigned work, airspace awareness, delivery activity, and system health."],operations:["Operations","Plan missions and keep field work moving."],projects:["Projects","Project Alpha projects with operational and delivery context."],tasks:["Tasks","Track the work required before, during, and after each mission."],airspace:["Airspace awareness","Official FAA TFR and special-use airspace information for Wisconsin."],delivery:["Client delivery","Browse the live R2 hierarchy and create secure client links."],team:["Team","Staff access, divisions, and role assignments."],administration:["Administration","Roles, integrations, synchronization, and audit history."]};return <div className="page-heading"><span className="eyebrow">LTDS Operations</span><h1>{copy[page][0]}</h1><p>{copy[page][1]}</p></div>}
function useLoad<T>(loader:()=>Promise<T>,deps:unknown[]=[]){const[data,setData]=useState<T|null>(null),[error,setError]=useState("");const reload=useCallback(()=>{setError("");return loader().then(setData).catch(caught=>setError(caught.message));},deps);useEffect(()=>{void reload();},[reload]);return{data,error,reload};}
function ErrorLine({error}:{error:string}){return error?<div className="notice error">{error}</div>:null;}

function Dashboard({session}:{session:Session}){const{data,error}=useLoad(()=>api<any>("/api/dashboard"),[]);if(!data&&!error)return<Loading/>;return <><ErrorLine error={error}/><div className="stats"><Card><strong>{data?.operations.length||0}</strong><span>Open operations</span></Card><Card><strong>{data?.tasks.length||0}</strong><span>Open tasks</span></Card><Card><strong>{data?.airspace.activeTfrs||0}</strong><span>Active TFRs listed</span></Card><Card><strong>{data?.airspace.activeSua||0}</strong><span>Active SUA reservations</span></Card></div><div className="dashboard-grid"><Card title="Upcoming operations">{data?.operations.length?<SimpleRows rows={data.operations.map((item:any)=>({title:item.title,detail:`${item.division_name} · ${date(item.scheduled_start)}`,status:item.status}))}/>:<EmptyState title="No open operations" detail="Scheduled work will appear here."/>}</Card><Card title="My work queue">{data?.tasks.length?<SimpleRows rows={data.tasks.map((item:any)=>({title:item.title,detail:date(item.due_at),status:item.status}))}/>:<EmptyState title="No open tasks" detail="Assigned work will appear here."/>}</Card><Card title="Integration health">{data?.integrations.map((item:any)=><div className="health-row" key={item.integration}><div><strong>{item.integration}</strong><small>{item.last_success_at?`Last success ${date(item.last_success_at)}`:"No successful sync yet"}</small></div><StatusPill tone={item.status==="healthy"?"success":item.status==="error"?"danger":"warning"}>{item.status}</StatusPill></div>)}</Card><Card title="Recent delivery activity">{data?.recentShares.length?<SimpleRows rows={data.recentShares.map((item:any)=>({title:`${item.client_name} · ${item.project_name}`,detail:item.r2_prefix,status:item.revoked_at?"revoked":"active"}))}/>:<EmptyState title="No delivery links" detail="Newly created client links will appear here."/>}</Card></div></>}
function SimpleRows({rows}:{rows:Array<{title:string;detail:string;status:string}>}){return <div className="simple-rows">{rows.map((row,index)=><div key={`${row.title}-${index}`}><div><strong>{row.title}</strong><small>{row.detail}</small></div><StatusPill tone={row.status==="blocked"||row.status==="revoked"?"danger":row.status==="active"||row.status==="ready"?"success":"neutral"}>{row.status.replaceAll("_"," ")}</StatusPill></div>)}</div>}

function Operations({session}:{session:Session}){const{data,error,reload}=useLoad(()=>api<{operations:any[]}>("/api/operations"),[]),[open,setOpen]=useState(false);return <><div className="section-actions">{allowed(session.user,"operations.manage")&&<button className="button-orange" onClick={()=>setOpen(!open)}>New operation</button>}</div>{open&&<OperationForm session={session} done={()=>{setOpen(false);void reload();}}/>}<ErrorLine error={error}/><Card className="table-card">{data?.operations.length?<table><thead><tr><th>Operation</th><th>Division</th><th>Schedule</th><th>Status</th></tr></thead><tbody>{data.operations.map(item=><tr key={item.id}><td><strong>{item.title}</strong><small>{item.project_name||"No linked project"}</small></td><td>{item.division_name}</td><td>{date(item.scheduled_start)}</td><td><StatusPill tone={item.status==="blocked"?"danger":item.status==="ready"||item.status==="completed"?"success":"neutral"}>{item.status.replaceAll("_"," ")}</StatusPill></td></tr>)}</tbody></table>:<EmptyState title="No operations yet" detail="Create the first mission when work is ready to schedule."/>}</Card></>}
function OperationForm({session,done}:{session:Session;done:()=>void}){const[error,setError]=useState("");return <Card title="Create operation"><form className="form-grid" onSubmit={async event=>{event.preventDefault();const raw=Object.fromEntries(new FormData(event.currentTarget));try{await api("/api/operations",{method:"POST",body:JSON.stringify({divisionId:raw.divisionId,title:raw.title,status:raw.status,scheduledStart:raw.scheduledStart?new Date(String(raw.scheduledStart)).toISOString():null,scheduledEnd:raw.scheduledEnd?new Date(String(raw.scheduledEnd)).toISOString():null,notes:raw.notes||null})});done();}catch(caught){setError((caught as Error).message);}}}><label>Division<select name="divisionId" required>{session.user.divisions.map(div=><option key={div.id} value={div.id}>{div.name}</option>)}</select></label><label>Title<input name="title" required/></label><label>Status<select name="status"><option value="draft">Draft</option><option value="scheduled">Scheduled</option><option value="ready">Ready</option><option value="blocked">Blocked</option></select></label><label>Start<input name="scheduledStart" type="datetime-local"/></label><label>End<input name="scheduledEnd" type="datetime-local"/></label><label className="full">Notes<textarea name="notes" rows={3}/></label><ErrorLine error={error}/><div className="full actions"><button className="button-orange">Create operation</button></div></form></Card>}

function Projects({session}:{session:Session}){const[search,setSearch]=useState("");const{data,error,reload}=useLoad(()=>api<{projects:any[]}>(`/api/projects${search?`?search=${encodeURIComponent(search)}`:""}`),[search]);return <><div className="section-actions"><input className="search" value={search} onChange={event=>setSearch(event.target.value)} placeholder="Search projects or clients"/><button className="button-ghost" onClick={()=>void reload()}>Refresh</button></div><ErrorLine error={error}/><div className="card-grid">{data?.projects.map(project=><Card key={project.id}><div className="project-card"><StatusPill>{project.status||"project"}</StatusPill><h3>{project.name}</h3><p>{project.customer_name}</p><small>{project.start_date?`Starts ${new Date(project.start_date).toLocaleDateString()}`:"No project date"}</small>{project.r2_prefix?<code>{project.r2_prefix}</code>:allowed(session.user,"delivery.browse")&&<FolderAssociation project={project} session={session}/>}</div></Card>)}</div>{data&&!data.projects.length&&<Card><EmptyState title="No matching projects" detail="Project Alpha projects will appear after a successful sync."/></Card>}</>}
function FolderAssociation({project,session}:{project:any;session:Session}){const[open,setOpen]=useState(false),[message,setMessage]=useState("");return open?<form className="folder-association" onSubmit={async event=>{event.preventDefault();const raw=Object.fromEntries(new FormData(event.currentTarget));try{await api(`/api/projects/${project.id}/folder`,{method:"POST",body:JSON.stringify({divisionId:raw.divisionId,r2Prefix:raw.r2Prefix})});setMessage("Folder linked. Refresh projects to see it.");}catch(caught){setMessage((caught as Error).message);}}}><select name="divisionId">{session.user.divisions.map(div=><option key={div.id} value={div.id}>{div.name}</option>)}</select><input name="r2Prefix" placeholder="jobs/2026/client/edited/" required/><button className="button-orange button-small">Link folder</button>{message&&<small>{message}</small>}</form>:<button className="button-ghost button-small" onClick={()=>setOpen(true)}>Associate delivery folder</button>}

function Tasks({session}:{session:Session}){const{data,error,reload}=useLoad(()=>api<{tasks:any[]}>("/api/tasks"),[]),[open,setOpen]=useState(false);return <><div className="section-actions">{allowed(session.user,"tasks.create")&&<button className="button-orange" onClick={()=>setOpen(!open)}>New task</button>}</div>{open&&<TaskForm session={session} done={()=>{setOpen(false);void reload();}}/>}<ErrorLine error={error}/><div className="kanban">{(["todo","in_progress","blocked","done"] as const).map(status=><Card key={status} title={status.replaceAll("_"," ")}><div className="task-stack">{data?.tasks.filter(task=>task.status===status).map(task=><article key={task.id}><strong>{task.title}</strong><p>{task.description||task.project_name||task.operation_title||"Operational task"}</p><small>{task.assigned_name||"Unassigned"} · {date(task.due_at)}</small></article>)}{data&&!data.tasks.some(task=>task.status===status)&&<span className="column-empty">No tasks</span>}</div></Card>)}</div></>}
function TaskForm({session,done}:{session:Session;done:()=>void}){const[error,setError]=useState("");return <Card title="Create task"><form className="form-grid" onSubmit={async event=>{event.preventDefault();const raw=Object.fromEntries(new FormData(event.currentTarget));try{await api("/api/tasks",{method:"POST",body:JSON.stringify({divisionId:raw.divisionId,title:raw.title,status:raw.status,description:raw.description||null,dueAt:raw.dueAt?new Date(String(raw.dueAt)).toISOString():null})});done();}catch(caught){setError((caught as Error).message);}}}><label>Division<select name="divisionId">{session.user.divisions.map(div=><option key={div.id} value={div.id}>{div.name}</option>)}</select></label><label>Task<input name="title" required/></label><label>Status<select name="status"><option value="todo">To do</option><option value="in_progress">In progress</option><option value="blocked">Blocked</option></select></label><label>Due<input name="dueAt" type="datetime-local"/></label><label className="full">Description<textarea name="description" rows={3}/></label><ErrorLine error={error}/><div className="full"><button className="button-orange">Create task</button></div></form></Card>}

function Airspace(){const{data,error,reload}=useLoad(()=>api<any>("/api/airspace/tfrs"),[]);return <><div className="safety-banner"><strong>Situational awareness only</strong><p>{data?.disclaimer||"Verify current FAA sources and NOTAMs before flight. This screen never represents a clearance."}</p></div><div className="section-actions"><button className="button-ghost" onClick={()=>void reload()}>Refresh view</button></div><ErrorLine error={error}/><div className="source-strip">{data?.sources.map((source:any)=><div key={source.source}><strong>{source.source}</strong><StatusPill tone={source.status==="fresh"?"success":source.status==="error"?"danger":"warning"}>{source.status}</StatusPill><small>{source.last_success_at?date(source.last_success_at):"No successful refresh"}</small></div>)}</div><div className="dashboard-grid">{data?.operationMatches?.length?<Card title="Operations requiring airspace review">{data.operationMatches.map((item:any)=><article className="airspace-row" key={`${item.operation_id}-${item.source_type}-${item.source_id}`}><div><strong>{item.operation_title}</strong><p>{item.source_title}</p></div><StatusPill tone={item.match_type==="intersects"?"danger":"warning"}>{item.match_type}</StatusPill></article>)}</Card>:null}<Card title="Temporary Flight Restrictions">{data?.tfrs.length?data.tfrs.map((item:any)=><article className="airspace-row" key={item.id}><div><strong>{item.notam_id} · {item.title}</strong><p>{item.description}</p><small>{item.geometry_available?"Mapped geometry":"Geometry unavailable"} · {date(item.effective_at)} to {date(item.expires_at)}</small></div><div><StatusPill tone={item.status==="active"?"danger":item.status==="scheduled"?"warning":"neutral"}>{item.status}</StatusPill><a href={item.official_url} target="_blank" rel="noreferrer">FAA record</a></div></article>):<EmptyState title="No listed Wisconsin TFRs" detail="Check the source freshness above; this is not a clearance."/>}</Card><Card title="MOA & special-use airspace">{data?.sua.length?data.sua.map((item:any)=><article className="airspace-row" key={item.id}><div><strong>{item.name}</strong><p>{item.airspace_type} · {item.low_altitude||"Lower altitude not listed"} to {item.high_altitude||"Upper altitude not listed"}</p><small>{item.status==="not_listed"?"No current reservation is listed; published operating hours may still apply.":`${date(item.starts_at)} to ${date(item.ends_at)}`}</small></div><StatusPill tone={item.status==="active"?"danger":item.status==="upcoming"||item.status==="pending"?"warning":"neutral"}>{item.status.replaceAll("_"," ")}</StatusPill></article>):<EmptyState title="No SUA schedule records" detail="A missing schedule is unknown, never assumed inactive."/>}</Card></div></>}

function Delivery({session}:{session:Session}){const[prefix,setPrefix]=useState(""),[view,setView]=useState<"grid"|"list">("grid"),[preview,setPreview]=useState<any>(null);const{data,error,reload}=useLoad(()=>api<any>(`/api/delivery/folders?prefix=${encodeURIComponent(prefix)}`),[prefix]);const crumbs=useMemo(()=>{const parts=prefix.split("/").filter(Boolean);let built="";return parts.map(name=>{built+=`${name}/`;return{name,prefix:built};});},[prefix]);return <><div className="delivery-tools"><nav><button onClick={()=>setPrefix("")}>All files</button>{crumbs.map(crumb=><span key={crumb.prefix}>/<button onClick={()=>setPrefix(crumb.prefix)}>{crumb.name}</button></span>)}</nav><button className={view==="grid"?"active":""} onClick={()=>setView("grid")}>Grid</button><button className={view==="list"?"active":""} onClick={()=>setView("list")}>List</button><button className="button-ghost" onClick={()=>void reload()}>Refresh</button></div><ErrorLine error={error}/><Card className="file-browser">{!data?<Loading/>:!data.folders.length&&!data.files.length?<EmptyState title="This folder is empty" detail="The view updates dynamically when TrueNAS syncs new content."/>:view==="grid"?<div className="file-grid">{data.folders.map((item:any)=><FolderCard key={item.prefix} item={item} open={()=>setPrefix(item.prefix)} share={allowed(session.user,"delivery.share.create")?()=>setPreview({shareFolder:item}):undefined}/>) }{data.files.map((item:any)=><FileCard key={item.id} item={item} preview={()=>setPreview(item)}/>)}</div>:<div className="file-list">{data.folders.map((item:any)=><button key={item.prefix} onClick={()=>setPrefix(item.prefix)}><span>▰</span><strong>{item.name}</strong><small>Folder</small></button>)}{data.files.map((item:any)=><button key={item.id} onClick={()=>setPreview(item)}><span>▧</span><strong>{item.name}</strong><small>{bytes(item.size)}</small></button>)}</div>}</Card>{preview?.shareFolder&&<ShareDialog folder={preview.shareFolder} close={()=>setPreview(null)}/>} {preview&&!preview.shareFolder&&<FilePreview item={preview} close={()=>setPreview(null)}/>}<ShareHistory session={session}/></>}
function FolderCard({item,open,share}:{item:any;open:()=>void;share?:()=>void}){return <article className="file-card"><button className="file-visual" onClick={open}><span className="folder-shape"/></button><strong>{item.name}</strong><small>Folder</small>{share&&<button className="share-chip" onClick={share}>Share</button>}</article>}
function FileCard({item,preview}:{item:any;preview:()=>void}){return <article className="file-card"><button className="file-visual" onClick={preview}>{item.thumbnailUrl?<img src={item.thumbnailUrl} loading="lazy" alt=""/>:<span>{item.kind}</span>}{item.kind==="video"&&<b className="play">▶</b>}</button><strong>{item.name}</strong><small>{bytes(item.size)} · {item.kind}</small><a className="download-chip" href={item.downloadUrl}>Download</a></article>}
function ShareDialog({folder,close}:{folder:any;close:()=>void}){const[result,setResult]=useState<any>(null),[error,setError]=useState(""),[expirationMode,setExpirationMode]=useState<"never"|"custom">("never");return <div className="modal-backdrop"><Card title="Share folder" action={<button className="button-ghost button-small" onClick={close}>Close</button>}><form className="form-grid" onSubmit={async event=>{event.preventDefault();const raw=Object.fromEntries(new FormData(event.currentTarget));try{setResult((await api<any>("/api/delivery/shares",{method:"POST",headers:{"Idempotency-Key":crypto.randomUUID()},body:JSON.stringify({r2Prefix:folder.prefix,accessCode:raw.accessCode||undefined,generateAccessCode:Boolean(raw.generateAccessCode),expiresAt:expirationMode==="custom"&&raw.expiresAt?new Date(String(raw.expiresAt)).toISOString():null})})).share);}catch(caught){setError((caught as Error).message);}}}><label>Expiration<select value={expirationMode} onChange={event=>setExpirationMode(event.target.value as "never"|"custom")}><option value="never">Never</option><option value="custom">Choose a date</option></select></label>{expirationMode==="custom"&&<label>Expires on<input name="expiresAt" type="datetime-local" required/></label>}<label>Access code<input name="accessCode" minLength={8}/></label><label className="check"><input name="generateAccessCode" type="checkbox"/> Generate a secure code</label><div className="full"><code>{folder.prefix}</code></div><ErrorLine error={error}/><div className="full"><button className="button-orange">Create secure link</button></div></form>{result&&<div className="share-result"><strong>Link created</strong><div><code><a href={result.shareUrl} target="_blank" rel="noreferrer">{result.shareUrl}</a></code><button className="button-ghost button-small" onClick={()=>navigator.clipboard.writeText(result.shareUrl)}>Copy</button></div>{result.accessCode&&<p>Access code: <code>{result.accessCode}</code></p>}</div>}</Card></div>}
function FilePreview({item,close}:{item:any;close:()=>void}){return <div className="modal-backdrop"><section className="preview"><header><strong>{item.name}</strong><a className="button button-orange button-small" href={item.downloadUrl}>Download</a><button className="button-ghost button-small" onClick={close}>Close</button></header><div>{item.kind==="image"?<img src={item.previewUrl} alt={item.name}/>:item.kind==="video"?<video src={item.previewUrl} controls autoPlay/>:(item.kind==="pdf"||item.kind==="text")?<iframe src={item.previewUrl} title={item.name}/>:<EmptyState title="Preview unavailable" detail="Download the original file to open it."/>}</div></section></div>}
function ShareHistory({session}:{session:Session}){const{data,error,reload}=useLoad(()=>allowed(session.user,"delivery.share.audit")?api<{shares:any[]}>("/api/delivery/shares"):Promise.resolve({shares:[]}),[]);if(!allowed(session.user,"delivery.share.audit"))return null;return <Card title="Recent client links" action={<button className="button-ghost button-small" onClick={()=>void reload()}>Refresh</button>}><ErrorLine error={error}/>{data?.shares.length?<table><thead><tr><th>Folder</th><th>Security</th><th>Status</th></tr></thead><tbody>{data.shares.map(share=><tr key={share.id}><td><code>{share.r2_prefix}</code></td><td>{share.password_protected?"Access code":"Link secret"}</td><td><StatusPill tone={share.revoked_at?"danger":"success"}>{share.revoked_at?"revoked":"active"}</StatusPill></td></tr>)}</tbody></table>:<EmptyState title="No delivery links" detail="Links created from the file browser appear here."/>}</Card>}

function Team({session}:{session:Session}){const{data,error,reload}=useLoad(()=>api<{staff:any[]}>("/api/admin/staff"),[]),[open,setOpen]=useState(false);return <><div className="section-actions">{allowed(session.user,"team.manage")&&<button className="button-orange" onClick={()=>setOpen(!open)}>Add staff account</button>}</div>{open&&<StaffForm session={session} done={()=>{setOpen(false);void reload();}}/>}<ErrorLine error={error}/><div className="card-grid">{data?.staff.map(person=><Card key={person.id}><div className="staff-card"><span>{person.display_name.slice(0,1).toUpperCase()}</span><h3>{person.display_name}</h3><p>{person.email}</p><small>{person.roles||"No role"}</small><small>{person.divisions||"Global or unassigned"}</small><StatusPill tone={person.status==="active"?"success":"danger"}>{person.status}</StatusPill></div></Card>)}</div></>}
function StaffForm({session,done}:{session:Session;done:()=>void}){const[error,setError]=useState("");return <Card title="Provision staff"><p className="muted">The same email must also be explicitly allowed in the Cloudflare Access application.</p><form className="form-grid" onSubmit={async event=>{event.preventDefault();const raw=Object.fromEntries(new FormData(event.currentTarget));try{await api("/api/admin/staff",{method:"POST",body:JSON.stringify({email:raw.email,displayName:raw.displayName,roleId:raw.roleId,scope:raw.scope,divisionId:raw.divisionId||null})});done();}catch(caught){setError((caught as Error).message);}}}><label>Name<input name="displayName" required/></label><label>Email<input name="email" type="email" required/></label><label>Role<select name="roleId"><option value="role-operator">Operator</option><option value="role-division-manager">Division Manager</option><option value="role-delivery-coordinator">Delivery Coordinator</option></select></label><label>Scope<select name="scope"><option value="division">Division</option><option value="assigned">Assigned work</option><option value="own">Own work</option></select></label><label>Division<select name="divisionId"><option value="">None</option>{session.user.divisions.map(div=><option key={div.id} value={div.id}>{div.name}</option>)}</select></label><ErrorLine error={error}/><div className="full"><button className="button-orange">Create staff account</button></div></form></Card>}

function Administration({session}:{session:Session}){const[message,setMessage]=useState("");const audit=useLoad(()=>allowed(session.user,"audit.view")?api<{events:any[]}>("/api/admin/audit"):Promise.resolve({events:[]}),[]);return <><div className="dashboard-grid"><Card title="Project Alpha"><p>Synchronizes workforce, business units, clients, organizations, projects, assignments, and service locations every 15 minutes.</p>{allowed(session.user,"integrations.manage")&&<button className="button-orange" onClick={async()=>{try{const result=await api<any>("/api/admin/integrations/project-alpha/sync",{method:"POST",body:"{}"});setMessage(`Sync complete: ${result.records} records processed.`);}catch(caught){setMessage((caught as Error).message);}}}>Sync now</button>}{message&&<div className="notice">{message}</div>}</Card><Card title="Security model"><p>Cloudflare Access authenticates staff. LTDS roles, explicit grants, resource scopes, and explicit denies authorize every API request.</p><ul><li>Missing permissions deny by default.</li><li>Cross-division resources return not found.</li><li>Privileged mutations are audited.</li></ul></Card></div>{allowed(session.user,"audit.view")&&<Card title="Audit history"><ErrorLine error={audit.error}/>{audit.data?.events.length?<table><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Resource</th></tr></thead><tbody>{audit.data.events.map(event=><tr key={event.id}><td>{date(event.created_at)}</td><td>{event.actor_display_name||event.actor_email||event.actor_type}</td><td><code>{event.action}</code></td><td>{event.entity_type} · {event.entity_id}</td></tr>)}</tbody></table>:<EmptyState title="No audit events" detail="Privileged changes will be recorded here."/>}</Card>}</>}
